# Aegisy 客户端使用指南

## 1. 登录

打开应用后，使用 Aegisy 邮箱和密码登录。密码框右侧按钮可切换显示或隐藏密码。勾选“记住登录状态”后，登录 Token 会保存到系统凭据库。登录成功后，右上角会显示账号余额并每分钟自动刷新，点击余额也可以立即刷新。

## 2. 新建单终端档案

点击“新建配置”，完成两步设置：

1. 输入配置名称，并在 Claude Code、Codex CLI、Gemini CLI 中选择一个终端。
2. 选择与该终端匹配的 API Key，并选择或输入模型。

一个档案只会保存一个终端的 Key 和模型。切换终端类型时，上一终端的 Key 和模型不会被复用。

保存成功后，应用会检测 Node.js 和对应 CLI。主界面侧栏会显示 Claude、Codex 和 Gemini 的本地安装版本，并可手动刷新。CLI 未安装时，点击侧栏“安装”或环境检查中的“一键安装环境”，应用会先补齐 Node.js，再安装对应 CLI。

## 3. 激活和切换

档案卡片显示终端类型、本地版本、脱敏 Key、模型和本地配置路径。点击“激活”后：

1. 建立当前终端配置的完整备份。
2. 合并并原子写入目标配置文件。
3. 校验写入后的 Key。
4. 任一步失败都会自动恢复写入前配置。

多数 CLI 需要重新启动后读取新配置。

Claude、Codex 和 Gemini 分别保存自己的当前档案。因此激活 Codex 配置只会替换当前 Codex 档案，不会取消已激活的 Claude 或 Gemini 配置；同一种终端始终只有一个档案处于激活状态。

关闭主窗口后应用会继续驻留系统托盘。托盘菜单可按终端直接切换档案、重新打开主窗口或彻底退出。

## 4. 备份和恢复

点击顶部“备份”进入历史列表。应用按 Claude、Codex、Gemini 分别保留最近 10 次备份。

恢复历史版本前，应用会再次备份当前配置。恢复后“当前使用”标记会清除，因为磁盘配置可能已经不再对应某个已保存档案。

## 5. 加密导入和导出

点击顶部“迁移”：

- “导出加密档案”会导出名称、终端、模型和 API Key。
- “导入加密档案”会解密、校验并追加档案。

导出文件使用 PBKDF2 + AES-256-GCM 加密，密码至少 8 个字符。密码错误、文件损坏或系统凭据库不可用时，导入不会写入任何档案。

## 6. API Keys 和模型

- “API Keys”用于查看账号 Key、配额、使用量和状态。
- “设为首选”只保存 Key ID，新建匹配终端的档案时会自动预选该 Key。
- “测试 Key”会调用模型接口检查 Key 是否可用。
- “模型”用于选择 Key、查询可用模型和复制模型名称。
- Key 列表自动分页，不受前 100 条限制。

## 7. 应用更新（macOS 和 Windows）

顶部“更新”菜单提供以下操作：

- “检查更新”会立即连接更新源，并显示新版本说明。
- “自动检查更新”默认开启，每 24 小时在后台检查一次。
- 下载完成后，macOS Sparkle 或 Windows WinSparkle 会验证 EdDSA 签名，再启动对应安装流程。

系统托盘菜单也可以直接执行“检查更新”。Linux 当前仍需要手动安装新版本。

## 8. 凭据存储

- Windows：DPAPI
- macOS：Keychain
- Linux：Secret Service（需要 `secret-tool`）

档案 API Key 不会写入普通 `QSettings`。Linux 没有 Secret Service 时，应用会拒绝保存凭据并显示错误。

macOS 会在首次读取已保存的登录状态或档案 API Key 时请求钥匙串授权。请选择“始终允许”。使用 ad-hoc 签名的测试安装包在每次更新后仍可能再次请求一次授权；正式发布包需要使用同一个 Apple Developer ID 持续签名，才能让系统跨版本稳定识别应用身份。

## 常见问题

### 档案无法保存

确认已选择 API Key。Linux 还需要安装并启动 Secret Service，例如：

```bash
sudo apt install libsecret-tools
```

### 激活失败

查看主窗口底部活动记录。配置写入失败时应用会自动回滚，错误信息会说明失败文件。

### 配置恢复后为什么没有“当前使用”档案

恢复的是磁盘历史配置，不一定与现有档案完全一致，因此应用会主动清除激活标记。重新激活需要的档案即可。

### 导出密码忘记了怎么办

无法恢复。AES-GCM 导出文件没有后门或找回机制，需要重新导出。

### 支持哪些工具

当前只支持 Claude Code、Codex CLI 和 Gemini CLI，不支持 Cursor、Continue.dev 或 OpenCode。

### 检查更新提示无法读取更新源

macOS 确认网络可访问 `https://aegisy.cc/desktop/macos/appcast.xml`，Windows 确认可访问 `https://aegisy.cc/desktop/windows/appcast.xml`。这些地址必须直接返回 Appcast XML，不能返回网站 HTML 页面。
